import { AccordionItemInterface } from "../interfaces/accoridon-item.interface";
import { Directive } from "@angular/core";

@Directive({})
export class AccordionItem<T> implements AccordionItemInterface<T> {
  
}
