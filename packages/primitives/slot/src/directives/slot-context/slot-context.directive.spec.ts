import { SlotContextDirective } from './slot-context.directive';

describe('SlotContextDirective', () => {
  it('should create an instance', () => {
    const directive = new SlotContextDirective();
    expect(directive).toBeTruthy();
  });
});
